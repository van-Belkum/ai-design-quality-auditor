required_phrases:
- ALL DIMENSIONS IN MM
forbidden_pairs:
- if_contains: Brush
  must_not_contain: Generator Power
- if_contains: Generator Power
  must_not_contain: Brush
allowlist:
- utilise
- utilised
- fenceline
suppress_patterns: []
